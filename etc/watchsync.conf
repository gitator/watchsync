# WatchSync Configuration File
# Global options inherited by all jobs

# Rsync options
rsync.delete = true
rsync.delay_ms = 500

# Global exclusions (one per line)
exclude = .git
exclude = node_modules
exclude = watchsync.*

# Remote credentials (can be set here or per-job)
# remote.password = your_password_here
